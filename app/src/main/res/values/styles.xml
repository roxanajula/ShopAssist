<resources>

    <color name="my_colorPrimary">#009688</color>
    <color name="my_colorPrimaryDark">#00796B</color>
    <color name="my_colorAccent">#ABA487</color>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/my_colorPrimary</item>
        <item name="colorPrimaryDark">@color/my_colorPrimaryDark</item>
        <item name="colorAccent">@color/my_colorPrimary</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

</resources>
